esphome:
  name: ${device_unit}-${device_board}-${device_number}
  friendly_name: "${friendly_unit_name}-${friendly_board_name} #${device_number}"
  comment: ${comment}
  project:
    name: ${device_author}.${device_unit}-${device_board}-${device_number}
    version: ${device_version}

wifi:
  ssid: ${wifi_ssid}
  password: ${wifi_password}
  ap:
    password: ${wifi_ap_password}

ota:
  platform: esphome
  password: ${ota_password}

api:
  encryption:
    key: ${api_key}

logger:

captive_portal:
